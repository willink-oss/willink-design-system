{
  "name": "registry",
  "version": "0.0.1",
  "private": true,
  "description": "Self-hosted shadcn-compatible registry for @willink blocks/components — emits static /r/*.json via `shadcn build` (ADR-0020). Deploy target: GitHub Pages /r/ (https://willink-oss.github.io/willink-design-system/r/); registry.i-willink.com planned as a future branded alias.",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit && shadcn build"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@willink-labs/react": "workspace:*",
    "clsx": "^2.1.1",
    "react": "^19.3.0",
    "shadcn": "^4.13.1",
    "tailwind-merge": "^3.7.0",
    "typescript": "^6.0.3"
  }
}
